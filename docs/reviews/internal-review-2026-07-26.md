# Внутренний состязательный ревью — 2026-07-26

**Это не аудит.** Документ написан тем же агентом, который написал проверяемый код
(`DuelEscrow v1.4`, holder-permits, сопутствующие изменения backend). Его слепые зоны
скоррелированы с ошибками, допущенными при написании, поэтому он не заменяет независимую
проверку и не является доказательством для `deployments/mainnet/release.json`. Файл намеренно
лежит вне `docs/audits/`, чтобы его нельзя было подставить в release evidence.

Назначение — сократить объём работы внешнего аудитора: закрыть то, что находится изнутри, и
честно перечислить то, что изнутри не проверяется.

| Поле      | Значение                                           |
| --------- | -------------------------------------------------- |
| Ревизия   | `5d42e4248968491eaa5d027362a71e39519fe9c1` и новее |
| Контракты | `BankQueue 1.3.0`, `DuelEscrow 1.4.0`              |
| Сеть      | TON testnet `-3`, production переключён на v1.4    |
| Метод     | чтение кода с гипотезами атак + исполняемые тесты  |

## Метод

Ревью строилось не вокруг чтения диффа, а вокруг гипотез: для каждой формулировалось, какое
конкретно наблюдаемое поведение опровергнет её. Там, где гипотеза проверяема исполнением, писался
тест — потому что сегодняшняя сессия дала прямое свидетельство границ чтения: три дефекта
(`F4`–`F6`) не были видны при многократном перечитывании и вскрылись только при реальном
развёртывании.

## Подтверждённые дефекты и их устранение

Все перечисленные найдены и исправлены в этой же сессии; каждый закрыт регресс-тестом.

| ID  | Дефект                                                                                  | Класс                 | Статус     |
| --- | --------------------------------------------------------------------------------------- | --------------------- | ---------- |
| F1  | `X-Content-Type-Options: nosn` — значение, которое браузеры игнорируют                  | защита выключена      | исправлено |
| F2  | no-reveal `ExpireDuel` оставлял `Duel.state='revealing'` навсегда                       | слепота мониторинга   | исправлено |
| F3  | referral qualification срабатывала только после DUEL settlement, не после BANK payout   | расхождение с текстом | исправлено |
| F4  | verifier сравнивал `holderFeeSupported` с `1`, TVM возвращает `-1`                      | ложный отказ          | исправлено |
| F5  | migration preflight читал `locked` как последний элемент стека; в v1.4 последний — флаг | ложная блокировка     | исправлено |
| F6  | скрипт паузы читал `contractConfig`, и v1.4-ABI перестал декодировать выведенный v1.3   | потеря owner-контроля | исправлено |

`F6` — самый неприятный по классу: инструмент владельца отказывал ровно в момент миграции, когда
старый контракт нужно закрыть. Переведён на `adminState`, форма которого одинакова во всех
развёрнутых версиях, поэтому дефект не воспроизведётся при следующей смене геттеров.

## Проверенные гипотезы атак (опровергнуты)

### Holder-permit

| Гипотеза                                                           | Результат                                                               |
| ------------------------------------------------------------------ | ----------------------------------------------------------------------- |
| Permit переносится на другой offer                                 | отвергнут: `offerId` в преобразе; тест на границе                       |
| Permit переносится на другой кошелёк                               | отвергнут: `owner` в преобразе; тест на границе                         |
| Permit переносится на другую сеть/deployment                       | отвергнут: `networkId` + `contract.getAddress()` в преобразе            |
| Просроченный permit молча даёт полную комиссию                     | отвергнут: открытие целиком падает, значение не блокируется             |
| Вечный permit                                                      | отвергнут: верхняя граница `now + MAX_EXPIRY_DELAY`                     |
| Повторное использование того же permit                             | невозможно: `usedOfferIds` не даёт переиспользовать offer id            |
| Коллизия с direct-accept permit (`0x4C4F4F62` против `0x4C4F4F63`) | разные домены в первых 32 битах преобраза                               |
| Освобождение меняет шанс или boost-математику                      | не меняет: `feeExempt` читается только в `settleWinner`                 |
| Освобождение ломает сохранение стоимости                           | не ломает: `locked` уменьшается ровно на `totalPool`, столько же уходит |

### Совместимость после переключения

Отдельно проверен сценарий, который на mainnet будет стоить дороже всего — **закешированный
клиент после cutover**. Тест `a stale v1.3 client body is rejected without touching state`
отправляет байт-в-байт тело v1.3 (без завершающего maybe-бита) в контракт v1.4 и доказывает:

- транзакция падает с cell underflow (exit `9`) **до** любой записи в storage;
- `locked` остаётся `0`, значение не захватывается;
- offer id не попадает в `usedOfferIds` — тот же кошелёк тут же успешно открывает тот же offer.

Адреса контрактов bounceable, поэтому значение возвращается отправителю. Практический вывод:
пользователь со старым бандлом видит ошибку кошелька и повторяет действие, потерять средства
этим путём нельзя.

## Что изнутри не проверяется

1. **Корректность самой конструкции.** Я проверял код против собственного замысла. Если замысел
   неверен, ревью этого не покажет.
2. **Живой льготный settlement.** Canary-кошельки не держат PLUSH BRICK, поэтому в сети доказан
   только полнокомиссионный путь. Нулевая комиссия доказана тестами контракта и тем, что живой
   getter объявляет поддержку, но не финализированной транзакцией.
3. **Экономическая устойчивость BANK.** Вне области кода.
4. **Соответствие законодательству.** Вне области кода и вне моей компетенции.
5. **Ответственность.** Отчёт без подписавшего его лица не даёт пользователю и регулятору того,
   на что можно сослаться.

## Открытые риски

Переданы в [audit-request-package.md](audit-request-package.md) как `R1`–`R6`; ни один не
блокирует testnet, все требуют решения владельца или подтверждения аудитором до mainnet.
